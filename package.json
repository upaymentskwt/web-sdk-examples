{
  "name": "upayments-web-sdk-examples",
  "version": "1.0.0",
  "private": true,
  "description": "Official merchant examples for UPayments Web SDK (UAPI)",
  "scripts": {
    "dev:react": "pnpm --filter @example/react-vite dev",
    "dev:vanilla": "pnpm --filter @example/vanilla-cdn dev",
    "build": "pnpm -r build"
  }
}
